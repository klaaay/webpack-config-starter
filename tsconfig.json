{
	"compilerOptions": {
		"baseUrl": ".",
		"outDir": "./dist", // 写不写都行
		"module": "commonjs", // 用 es6 模块引入 import
		"target": "es5", // 打包成 es5
		"sourceMap": true,
		"allowJs": true, // 允许在 ts 中也能引入 js 的文件
		"jsx": "react"
	}
}